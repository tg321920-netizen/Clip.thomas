# ClipForge Multi — local / worker configuration

# Optional. Leave blank to use "ffmpeg" from PATH.
FFMPEG_PATH=

# Optional. Leave blank to use "ffprobe" from PATH.
FFPROBE_PATH=

# Optional. Leave blank to use ./storage.
# In a worker, point this to a writable persistent volume.
CLIPFORGE_STORAGE_DIR=

# Optional. Whisper CLI executable name/path.
# Install OpenAI Whisper in the worker environment; no API key is required
# for this local provider. Blank defaults to "whisper".
WHISPER_COMMAND=

# Optional. Local Whisper model. Blank defaults to "base".
WHISPER_MODEL=

# Optional. ISO language code such as es/en/fr.
# Blank lets Whisper auto-detect the language.
WHISPER_LANGUAGE=

# Optional. Worker polling interval in milliseconds. Blank defaults to 2000.
CLIPFORGE_WORKER_POLL_MS=

# Optional. Autopilot orchestration polling interval in milliseconds.
# Blank defaults to 10000.
CLIPFORGE_AUTOPILOT_POLL_MS=

# Optional. Publishing worker polling interval in milliseconds.
# Blank defaults to 30000.
CLIPFORGE_PUBLISHING_POLL_MS=

# Required before server-side OAuth credentials can be persisted.
# Must be a base64-encoded 32-byte random key. Keep it only in the server/worker
# secret store. Never expose it to the browser or commit a real value.
CLIPFORGE_CREDENTIALS_KEY=

# Required by the Facebook publishing adapter when it is eventually connected.
# Set this to the Graph API version approved/current for your Meta app.
# No default is hardcoded so ClipForge does not silently pin an obsolete version.
META_GRAPH_API_VERSION=

# Optional. "heuristic" (default) works locally without an external model.
# Set to "openai" to enable the OpenAI Responses API provider.
CLIPFORGE_ANALYSIS_PROVIDER=heuristic

# Required only when CLIPFORGE_ANALYSIS_PROVIDER=openai.
# Create the key in the OpenAI API dashboard and keep it server-side.
OPENAI_API_KEY=

# Required only when CLIPFORGE_ANALYSIS_PROVIDER=openai.
# Set this to a model available to your OpenAI API project.
CLIPFORGE_AI_MODEL=

# Optional. Defaults to https://api.openai.com/v1.
OPENAI_BASE_URL=

# Optional. Auto Edit provider: "heuristic" (default) or "openai".
CLIPFORGE_AUTOEDIT_PROVIDER=heuristic

# Optional. Model used only when CLIPFORGE_AUTOEDIT_PROVIDER=openai.
# If blank, ClipForge falls back to CLIPFORGE_AI_MODEL.
CLIPFORGE_AUTOEDIT_MODEL=
